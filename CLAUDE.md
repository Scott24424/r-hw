# r-hw

## 프로젝트 개요

- **r-hw**: 남은 방학숙제 관리 웹앱
- **스택**: Next.js(App Router) + TypeScript + Prisma + SQLite
- **설계 문서**: 구현 전 아래 두 문서를 **항상 먼저, 둘 다** 확인한다.
  - `docs/architecture.md` — **무엇을 만드는가.** 데이터 모델, 상태 전이, 진도 해석 규칙, API, 화면, 테스트 대상
  - `docs/decisions.md` — **왜 그렇게 정했는가.** 설계 결정별 선택 / 기각한 대안 / 이유 / 남는 대가
  - 두 문서는 짝이다. `architecture.md` 본문의 `(D5)` 같은 표기는 `decisions.md`의 항목을 가리키므로, 결정의 근거가 필요하면 그쪽을 본다.
  - 설계와 다르게 구현해야 할 이유를 발견하면 임의로 바꾸지 말고 멈추고 질문한다. 결정이 바뀌면 `decisions.md`의 해당 항목도 함께 갱신한다.

## v3 워크플로우

이 프로젝트는 v-hw 프로젝트에서 검증된 v3 워크플로우를 따른다.

| 역할 | 담당 |
|---|---|
| 설계 · 작업 분해 · 스펙 작성 · 코드 리뷰 | Claude Code |
| 구현 · 테스트 작성 · PR 생성 · 리뷰 수정 반영 | Codex (별도 에이전트, `AGENTS.md`를 읽음) |

**원칙: 산출물 작성자와 검증자는 항상 다른 에이전트다.** Claude Code가 설계/스펙을 작성하면 Codex가 구현하고, Claude Code가 그 결과를 리뷰한다. 같은 에이전트가 자신의 산출물을 검증하지 않는다.

`AGENTS.md`는 이 파일(`CLAUDE.md`)의 심볼릭 링크다 — 두 에이전트가 동일한 규칙을 읽는다.

## 구현 규칙 (스펙 기반)

- 구현 태스크는 `docs/specs/T##-*.md` 스펙 파일이 있을 때만 착수한다.
- 스펙에 명시되지 않은 판단이 필요하면 임의로 진행하지 않고 멈추고 질문한다.
- 스펙 범위를 벗어난 파일은 수정하지 않는다.
- 구현 완료 보고에는 실행한 명령과 실제 출력(테스트 결과 포함)을 반드시 포함한다.

## 브랜치 규칙

- 작업 시작 전 현재 브랜치를 확인한다. `main`이면 반드시 새 브랜치를 만든 뒤 작업한다.
- 브랜치명: `feature/T##-요약`, `fix/T##-요약`, `chore/요약`, `docs/요약`
- `main` 직접 커밋 금지.
- `git push`는 사용자 승인 후에만 실행한다.

## 개발 규칙

- 커밋은 Conventional Commits 형식 (`feat:`, `fix:`, `test:`, `docs:`, `chore:`)
- 새 기능은 단위 테스트와 함께 작성한다. 정상 케이스와 규칙 위반 케이스를 모두 포함한다.
- `any` 타입 금지. 타입 판단이 어려우면 진행하지 말고 질문한다.
- 커밋 전 lint와 test를 실행한다.

## 금지 사항

- `.env` 파일에 더미/가짜 값 삽입 금지. 값이 없으면 사용자에게 질문한다.
- `.env` 파일 읽기 · 수정 금지.
- 마이그레이션 파일 직접 수정 금지. `prisma migrate`를 사용한다.
- 확신이 없을 때 추측으로 진행하지 않는다. 멈추고 질문한다.

## 코드 리뷰 대응

- 리뷰 지적을 받으면 먼저 코드로 타당성을 확인한 뒤 수정한다.
- 지적이 타당하지 않다고 판단되면 근거를 제시하고 수정하지 않는다.
